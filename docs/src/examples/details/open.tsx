import { Details } from '@nordcom/nordstar';

export default function Example() {
    return (
        <Details label="Already open" open>
            Pass the native `open` attribute to start expanded.
        </Details>
    );
}
