import { Details } from '@nordcom/nordstar';

export default function Example() {
    return (
        <Details label="What does this do?">
            And this is the content inside the Details component, passed through children.
        </Details>
    );
}
