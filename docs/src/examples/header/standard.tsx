import { Header } from '@nordcom/nordstar';

export default function Example() {
    return (
        <Header sticky={false}>
            <Header.Logo>Nordstar</Header.Logo>
        </Header>
    );
}
