{
    "$schema": "https://json.schemastore.org/tsconfig.json",
    "extends": "../../../tsconfig.json",
    "compilerOptions": {
        "baseUrl": "."
    },
    "include": ["src/**/*"],
    "exclude": ["src/**/*.stories.*", "src/**/*.test.*"]
}
