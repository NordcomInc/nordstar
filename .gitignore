# dependencies
node_modules
/.pnp
.pnp.js

# testing
/coverage
coverage

# next.js
/.next/
.next/
/out/
docs/out/

# playwright
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# production
/build
dist
*.tsbuildinfo
*ts.timestamp-*

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
.claude/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
packages/**/*.backup
packages/**/*.backup.ts

# deployment
.sentryclirc
.vercel

# ignore sitemap
apps/**/sitemap.xml

# turbo
.turbo/
packages/**/.turbo

# content layer
.contentlayer

# generated docs data — regenerated by `pnpm generate` (predev/prebuild/pretypecheck).
# Committed counterparts (props/examples) are tiny; this one is multi-MB and would
# churn on every release, so it is generated on demand instead of tracked.
docs/src/content/changelogs.generated.json
