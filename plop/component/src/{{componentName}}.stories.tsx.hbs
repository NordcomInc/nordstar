import { {{capitalize componentName}} } from '@nordcom/nordstar-{{componentName}}';
import type { {{capitalize componentName}}Props } from '@nordcom/nordstar-{{componentName}}';
import type { Meta, StoryObj } from '@storybook/react';

const story: Meta<typeof {{capitalize componentName}}> = {
    title: 'System/Components/{{capitalize componentName}}',
    component: {{capitalize componentName}},
    argTypes: {}
};

export default story;
type Story = StoryObj<typeof {{capitalize componentName}}>;

const Template = (args: {{capitalize componentName}}Props) => <{{capitalize componentName}} {...args} />;

export const Standard: Story = {
    render: Template,
    args: {}
};
